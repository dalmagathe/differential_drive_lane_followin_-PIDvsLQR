# Point de départ commun : robot Gazebo (spawn) ET estimateur odométrie.
# Lu par gazebo.launch.py (args de spawn) et control.launch.py (params estimateur).
initial_pose:
  x: 0.0
  y: -0.4     # offset latéral initial [m] — l'erreur à corriger
  theta: 0.0   # cap initial [rad]